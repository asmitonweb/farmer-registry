# Byte-compiled / optimized / DLL files
__pycache__/
*.py[codz]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
# Anchored to the root: an unanchored `lib/` also swallowed dashboard-ui/lib/.
/lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# PyInstaller
#  Usually these files are written by a python script from a template
#  before PyInstaller builds the exe, so as to inject date/other infos into it.
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py.cover
.hypothesis/
.pytest_cache/
cover/

# Translations
*.mo
*.pot

# Logs
*.log

# Flask stuff:
instance/
.webassets-cache

# Sphinx documentation
docs/_build/

# PyBuilder
.pybuilder/
target/

# Jupyter Notebook
.ipynb_checkpoints

# IPython
profile_default/
ipython_config.py

# pyenv
#   For a library or package, you might want to ignore these files since the code is
#   intended to run in multiple environments; otherwise, check them in:
# .python-version

# pipenv / poetry / pdm — keep lock files in VCS (library convention)
#Pipfile.lock
#poetry.lock
#pdm.lock
.pdm-python
.pdm-build/

# uv
#uv.lock

# pixi
.pixi

# PEP 582; used by e.g. github.com/David-OConnor/pyflow and github.com/pdm-project/pdm
__pypackages__/

# Celery stuff
celerybeat-schedule
celerybeat.pid

# SageMath parsed files
*.sage.py

# Environments
.env
.env.*
!.env.example
# The local compose stack's env file. Checked in deliberately: it holds no real
# credentials, only the local-only values that must match local/postgres/init.sql,
# and `docker compose --env-file local/.env` does not work without it.
!local/.env
.envrc
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# FastAPI / Uvicorn / Gunicorn runtime
*.pid
*.sock

# Local databases (dev SQLite, dumps)
*.sqlite
*.sqlite3
*.sqlite3-journal
*.db
*.dump
*.sql.bak

# Spyder project settings
.spyderproject
.spyproject

# Rope project settings
.ropeproject

# mkdocs documentation
/site

# mypy
.mypy_cache/
.dmypy.json
dmypy.json

# Pyre type checker
.pyre/

# pytype static type analyzer
.pytype/

# Cython debug symbols
cython_debug/

# Ruff
.ruff_cache/

# PyPI configuration file
.pypirc

# IDEs
.idea/
.vscode/
*.swp
*.swo
*~

# OS files
.DS_Store
Thumbs.db
Desktop.ini

# Cursor
.cursorignore
.cursorindexingignore

# Claude Code (local runtime state — scheduled-task locks etc.)
.claude/

# Docker build artifacts (generated by docker/scripts/build.sh and the CI workflows)
/docker/adapters.requirements.txt
/docker/scripts/_service_env.sh
/docker/scripts/.env
# farmer-registry-dashboard-api, cloned by the Jenkinsfile
/.build/

# ---------------------------------------------------------------------------
# Helm artifacts
# ---------------------------------------------------------------------------
# Chart.lock is regenerated by `helm dependency update/build`. Because this
# wrapper chart pins to a `-develop` tag of openg2p-registry (an ever-moving
# target), the lock provides no real determinism — leave it untracked.
helm/**/Chart.lock

# Subchart tarballs downloaded by `helm dependency build` into <chart>/charts/.
# Vendored uncompressed subcharts (charts/<name>/) remain tracked.
helm/**/charts/*.tgz
